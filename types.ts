// Copyright 2023-latest the httpland authors. All rights reserved. MIT license.
// This module is browser compatible.

import { Token } from "./deps.ts";

/** HTTP `Accept-Ranges` header field. */
export type AcceptRanges = [Token, ...Token[]];
